{
	email {$ACME_EMAIL:}
	admin off
}

{$PROJECTLECTURE_DOMAIN} {
	encode zstd gzip

	reverse_proxy web:8000

	header {
		X-Content-Type-Options nosniff
		Referrer-Policy same-origin
		-Server
	}

	log {
		output stdout
		format json
	}
}
